<nav class="navbar navbar-default"  style="margin-top:10px;" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Web Reg <sup> 2.0 </sup></a>
    </div>

    <div class="navbar-nav navbar-right" style="margin-right: 0px;">
      {{#if notloggedin}}
      <button class="btn btn-default navbar-btn" data-toggle="modal" data-target="#login">Login</button>
      <button class="btn btn-default navbar-btn" onclick="location.href = ('/signup');" >Sign up</button>
      {{/if}}
      {{#if loggedin}}
      <button class="btn btn-default navbar-btn" data-bind="click: logout">Log Out </button>
      {{/if}}
   </div>
 </div><!-- /.container-fluid -->
</nav>